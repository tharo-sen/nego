# Optional: enables LLM-polished replies and advice.
# Without it, the built-in rule-based negotiation engine is used (works fully offline).
OPENAI_API_KEY=
# Optional: override the model used when OPENAI_API_KEY is set.
OPENAI_MODEL=gpt-4o-mini
